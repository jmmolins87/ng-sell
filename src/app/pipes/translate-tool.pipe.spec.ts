import { TranslateToolPipe } from './translate-tool.pipe';

describe('TranslateToolPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateToolPipe();
    expect(pipe).toBeTruthy();
  });
});
